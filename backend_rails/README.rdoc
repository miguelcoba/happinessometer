== Happinessometer

Para instalar el proyecto y correrlo:

1. Instalar RVM y Ruby:
$ \curl -sSL https://get.rvm.io | bash -s stable --ruby

Y hacer el source que dice en el output de la instalación

2. Instalar Rails:
$ gem install rails

Y esperar una eternidad

3. Instalar los gems:
$ cd <project root>/backend_rails
$ bundle install
